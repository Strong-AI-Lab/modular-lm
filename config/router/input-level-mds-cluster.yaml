router_name: "input-mds-kmeans-cluster"
router_path: "MDSInputLevelCluster"
pretrain_cluster: true
pretrain_batch_size: 4
routing_strategy:
  clustering_algorithm: "K-Means"
  num_embeddings: 2 # Must be equal to "nb_modules" in model config
  training_latents: null
  reduction_components: 3
  reduction_training_memory_size: 128
